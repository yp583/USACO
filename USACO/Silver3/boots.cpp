#include <bits/stdc++.h>
using namespace std;
int main(){
    int n, b; cin >> n, b;
    int snow[n];
    int boots[b][2];
    for (int i = 0; i < n; i++)
    {
        cin >> snow[i];
    }
    for (int i = 0; i < b; i++)
    {
        cin >> boots[i][0] >> boots[i][1];
    }
    
    pos = 0;
    for (int i = 0; i < b; i++)
    {
        while (pos < n){
            pos + boots[1]
        }
    }
    

}