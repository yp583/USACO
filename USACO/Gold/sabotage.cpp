#include <bits/stdc++.h>
using namespace std;

bool ok(int avg){
    for (int i = 0; i < n; i++)
    {
        for (int j = 0; j < n; j++)
        {
            /* code */
        }
        
    }
    
}

int main(){
    int n; cin>>n;
    vector<int> mach;
    for (int i = 0; i < n; i++)
    {
        int t; cin>>t;
        mach.push_back(t);
    }  
}