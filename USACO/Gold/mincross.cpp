#include <bits/stdc++.h>
using namespace std;

int main(){
    int n; cin >> n;
    int fields1[n];
    int fields2[n];

    for (int i = 0; i < n; i++)
    {
        cin >> fields1[i];
    }
    for (int i = 0; i < n; i++)
    {
        cin >> fields2[i];
    }
    
}