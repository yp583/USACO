#include <bits/stdc++.h>
using namespace std;

int main(){
    int n, m, k; cin >> n >> m >> k;
    string s; cin >> s;
    int adjmatrix[m][m];
    for (int i = 0; i < m; i++)
    {
        for (int j = 0; j < m; j++)
        {
            cin >> adjmatrix[i][j];
        }
    }
    
}